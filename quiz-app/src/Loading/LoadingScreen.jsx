const { ExposureOutlined } = require("@material-ui/icons");

const Loading = () => {
    return(
        <main>
            <div className="loading">
            </div>
        </main>
    );
};

export default Loading;